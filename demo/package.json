{
  "name": "suitest-js-api-demo",
  "version": "1.0.0",
  "description": "Demo set up for Suitest JS API",
  "private": true,
  "dependencies": {
    "mocha": "^5.0.4",
    "suitest-js-api": "^1.1.0"
  },
  "scripts": {
    "interactive": "suitest interactive mocha --no-timeouts --exit",
    "automated": "suitest automated mocha --no-timeouts --exit"
  }
}
