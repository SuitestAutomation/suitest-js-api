import * as suitest from '../../index';

suitest.startTestPack({
	accessTokenKey: 'string',
	accessTokenPassword: 'string',
	testPackId: 0,
	config: {
		configVariables: [{
			key: 'string',
			value: 'string'
		}]
	}
});

suitest.setAppConfig('string', {
	configVariables: [{
		key: 'string',
		value: 'string'
	}]
});
