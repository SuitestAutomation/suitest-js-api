import { element } from '../../index';

const el = element({
    css: '.my-element',
});
el.click();
el.exist();
el.moveTo();
el.sendText('');
el.timeout(1);
el.exists();
el.backgroundColor;
el.text;
el.id;

