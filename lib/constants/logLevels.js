/**
 * Logger verbosity levels
 */

const logLevels = {
	silent: 'silent',
	normal: 'normal',
	verbose: 'verbose',
	debug: 'debug',
	silly: 'silly',
};

Object.freeze(logLevels);

module.exports = logLevels;
