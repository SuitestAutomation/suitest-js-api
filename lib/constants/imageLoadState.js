/* eslint-disable key-spacing */

const imageLoadState = {
	LOADED: 'loaded',
	LOADING: 'loading',
	ERROR: 'error',
	UNKNOWN: 'unknown',
	EMPTY: 'empty',
};

Object.freeze(imageLoadState);

module.exports = imageLoadState;

